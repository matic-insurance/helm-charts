apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "clamav-api.fullname" . }}-test-connection"
  labels:
    {{- include "clamav-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "clamav-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
