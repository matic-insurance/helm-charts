{{- if .Values.egress.enabled }}
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: istio-egress-gateway-disable-mtls
spec:
  host: istio-egress-gateway.istio-ingress.svc.cluster.local
  exportTo:
  - "."
  trafficPolicy:
    tls:
      mode: DISABLE
{{- end }}
