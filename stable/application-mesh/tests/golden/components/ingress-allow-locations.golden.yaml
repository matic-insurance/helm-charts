---
# Source: application-mesh/templates/virtual-service.mesh.yaml
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: best-product-backend
  labels:
    app.kubernetes.io/part-of: best-product
    app.kubernetes.io/version: "0.0.0"
spec:
  hosts:
    - "best-product-backend.app-component-test00000.svc.cluster.local"
  gateways:
    - mesh
  http:
    - name: allow-all-locations
      route:
        - destination:
            host: "best-product-backend.app-component-test00000.svc.cluster.local"
            subset: all-versions
---
# Source: application-mesh/templates/virtual-service.gateway.yaml
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: best-product-773e0944
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "best-hooks-st1.example.com, product-st1.example.com, abc-hooks-st1.example.com"
    external-dns.alpha.kubernetes.io/ttl: "60"
  labels:
    app.kubernetes.io/part-of: best-product
    app.kubernetes.io/version: "0.0.0"
spec:
  hosts:
    - "best-hooks-st1.example.com"
    - "product-st1.example.com"
    - "abc-hooks-st1.example.com"
  gateways:
    - istio-ingress/external-example-com
  http:
    - name: allow-ingress-spec
      match:
        - uri:
            prefix: /api/hooks
        - uri:
            prefix: /api/status
      route:
        - destination:
            host: "best-product-backend.app-component-test00000.svc.cluster.local"
            subset: all-versions
          
    - name: deny-everything-except-allowed
      directResponse:
        status: 403
        body:
          string: Access Denied