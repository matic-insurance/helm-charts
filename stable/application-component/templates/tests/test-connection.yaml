apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "application-component.fullname" . }}-test-connection"
  labels:
    {{- include "application-component.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "application-component.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
