global:
  application:
    product: myproduct
    environment: test
    version: latest

  applicationImage:
    repository: "bitnami/ruby"

  mesh:
    enabled: false

component: webserver
command: ruby -e "loop { puts 'working'; sleep 1}"

serviceAccount:
  name: default
